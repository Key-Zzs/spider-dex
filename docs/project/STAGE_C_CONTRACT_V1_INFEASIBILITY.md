# Stage C Contract V1: immutable infeasibility record

Contract V1 is `EXACT_SOURCE_FINGER_CONTACT`.  Its primary frozen sequence is
`s5/cylindermedium_lift [1460,1876)`.  The result is permanently
`BLOCKED_BY_INFEASIBLE_EMBODIMENT_CONTACT`.

This conclusion is not a request to lower V1's `0.70` same-finger recall
threshold, alter its denominator, delete source contacts, move the object, or
replace the frozen pilot.  It remains in force even if Contract V2 succeeds.

The machine-readable, hash-addressed manifest is external so no generated
trajectory or raw data enters Git:

`<workspace>/reports/stage_c_contract_v1_manifest.json`.

Its evidence includes the V1 contract/profile hashes, frozen pilot, bounded
multi-start Pareto, active joint-limit sets, real-GPU MJWP sanity run, original
infeasibility report, and Stage C validation report.  V2 consumers must quote
this status verbatim: **V1 result is immutable and remains BLOCKED.**
